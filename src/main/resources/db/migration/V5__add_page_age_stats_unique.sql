-- ============================================================
-- V5: page_age_stats (page_id, age_band) 유니크 제약은 V1 CREATE TABLE에 포함됨
-- 이 파일은 기존 DB 호환을 위해 유지 (no-op)
-- ============================================================
